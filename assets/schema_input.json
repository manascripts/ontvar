{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Sample sheet schema for ONT SV calling",
  "description": "Defines required columns for SV calling pipeline with matched sample pairing",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["sample_id", "bam_path", "sample_type", "match_id"],
    "properties": {
      "sample_id": {
        "type": "string",
        "pattern": "^\\S+$",
        "errorMessage": "Sample ID must be provided and cannot contain spaces"
      },
      "bam_path": {
        "type": "string",
        "format": "file-path",
        "exists": true,
        "pattern": "^.*\\.(bam|cram)$",
        "errorMessage": "BAM path must be a valid .bam file"
      },
      "sample_type": {
        "type": "string",
        "enum": ["case", "control"],
        "errorMessage": "sample_type must be either 'case' or 'control'"
      },
      "match_id": {
        "type": "string",
        "pattern": "^\\S+$",
        "errorMessage": "match_id must be provided and cannot contain spaces"
      }
    }
  }
}
